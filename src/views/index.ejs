<!DOCTYPE html>
<html lang="en">

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Controle estudantes</title>
</head>

<body class="body-index">
    <header class="fixed-top w-100">
        <div
            class="d-flex flex-column flex-md-row align-items-center justify-content-between md-5 p-3 px-md-4 mb-3 bg-white border-bottom shadow">
            <a href="/">
                <h5 class="my-0 mr-md-auto font-weight-normal">Bosch |
                    <span style="color:#595959; font-weight: 900;">Controle</span>
                    <span style="color:#595959; font-weight: 500;">de Estudantes</span>
                </h5>
            </a>

            <nav class="my-2 my-md-9 mr-md-3">
                <a class="p-2 text-dark" href="/CadastroAluno">Alunos</a>
                <a class="p-2 text-dark" href="/CadastroSala">Sala</a>
            </nav>
        </div>
    </header>
    <main>
        <div class="form-floating d-flex w-25 mb-3 ms-5">
            <form action="/" method="post">
            <select id="sala" class="form-select" aria-label="Floating label select example"
                onchange="this.form.submit()" name="nome">

                <option value="">Selecione uma sala</option>

                <% for (let i=0; i<salas.length; i++) { %>
                    <option value='<%= salas[i].IDSala %>' <%= id == salas[i].IDSala ? 'selected' : ''%>> <%= salas[i].Nome %> </option>
                <% } %>

            </select>
        </form>
        </div>
        <div class="d-flex p-2 flex-wrap justify-content-center">
            <%for (let i=0; i<alunos.length; i++){%>
                <div class="card  shadow-sm p-3 mb-5 bg-body-tertiary rounded align-items-center m-5 mt-0"
                    style="width: 25rem;">
                    <img src="img/<%= alunos[i].Foto %>" class="card-img-top rounded-circle w-50 p-3" alt="Foto Aluno">
                    <div class="card-body text-center">
                        <h5 class="card-title">Nome: <%= alunos[i].Nome %></h5>
                        <p class="card-text">Idade: <%= alunos[i].Idade %></p>
                        <form action="/editarAluno/<%= alunos[i].IDAluno %>" method="get">
                            <button type="submit" class="btn btn-primary"> Editar</button>
                        </form>
                    </div>
                </div>
            <%}%>
        </div>
    </main>
    <footer style="background-color: bisque">
        <div
            class="d-flex flex-column flex-md-row align-items-center justify-content-between md-5 p-3 px-md-4 border-bottom shadow foo">
            <h5 class="my-0 mr-md-auto font-weight-normal">Bosch
                <span style="color:#595959; font-weight: 900;">Engineering</span>
                <span style="color:#595959; font-weight: 500;">Technical School</span>
            </h5>
        </div>
    </footer>
</body>

</html>